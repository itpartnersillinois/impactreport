<!doctype html>
<html lang="en">
    <head>
        {% include head.liquid %}
        <title>{{ pageTitle }} | Impact Report 2022 | College of Education | U of I</title>
    </head>
    <body class="{{ bodyClass }}">
        {% include menu.liquid %}
        <main class="splash il-formatted" style="background-image: url(../img/backgrounds/{{ backgroundImage }});">
        <div class="full">
        <h1 class="intro-text">{{ pageTitle }} </h1>
        <ul class="stats no-bullets">
        {% for stat in stats %}
            <li>{{ stat | transformStat }}</li>
        {% endfor %}
        </ul>
        {{ content }}        
        </div>
        </main>
    </body>
</html>