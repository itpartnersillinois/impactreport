<!doctype html>
<html lang="en">
    <head>
        {% include head.liquid %}
    </head>
    <body class="{{ bodyClass }}">
        {% include menu.liquid %}
        <main class="article il-formatted">
        <h1>{{ pageTitle | transformTitle: titleStart, titleEnd }}</h1>
        <p class="byline">{{ byline }}</p>
        {{ content }}
        </main>
    </body>
</html>